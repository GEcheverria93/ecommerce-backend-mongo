<div id='cart-container'>
    <ul id='cart-list'>
        {{#each carts}}
            <li>
                <h2>Carrito ID: {{id}}</h2>
                <ul>
                    {{#each products}}
                        <li>
                            Producto:
                            {{title}}
                            - Cantidad:
                            {{quantity}}
                            - Precio: ${{price}}
                        </li>
                    {{/each}}
                </ul>
                <div class='total-price'>
                    Total: ${{calculateTotal products}}
                    <!-- Calcular el total del carrito -->
                </div>
                <button class='btn-buy' data-cart-id='{{id}}'>Comprar</button>
                <button class='btn-clear' data-cart-id='{{id}}'>Limpiar carrito</button>
            </li>
        {{/each}}
    </ul>
</div>

<link rel='stylesheet' href='/css/cart.css' />
<script src='/js/cart.js' defer></script>